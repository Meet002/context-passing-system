This project demonstrates a Context Passing Strategy for AI interview systems.

Features:
- Stores interview history
- Keeps only recent context
- Generates evaluation summary
- Prevents token overload
- Builds smart context for LLM

Technologies Used:
- Python
- JSON